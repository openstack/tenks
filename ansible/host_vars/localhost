---
# FIXME(w-miller): set this more cleverly?
base_path: "~/tenks/"
allocations_file_path: "{{ base_path + '/allocations.yml' }}"

flavours:
  flav0:
    memory_mb: 4096
    vcpus: 4
    volumes:
      - capacity: 10GB
    physical_networks:
      - physnet1

specs:
  flav0: 4
